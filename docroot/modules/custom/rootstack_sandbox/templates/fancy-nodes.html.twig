<div class="row">
    <div class="col-sm-12">
        <div class="jumbotron">
            {{ summary }}
        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-8">
        <ul class="list-group">
            {% for nid, node in some_nodes %}
                <li class="list-group-item">
                    {#<a href="{{ path('node/' ~ nid, item.url.routeParameters, item.url.options) }}">{{ item.title }}</a>#}
                    {#{{  path('sadasd', {'node': node}) }}#}

                    {#{{ link(node, 'node/' ~ nid) }}#}
                    {{ node }}
                </li>
            {% endfor %}
        </ul>
    </div>
    <div class="col-md-4">
        <div class="well">
            Total of nodes: {{ total }}
        </div>
    </div>
</div>